<div class="quote bg-slate-300">
  <%= link_to quote.name, quote_path(quote) %>
  <div class="quote__actions mb-2">
    <%= link_to "Delete", quote, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    <%= link_to "Edit",
                edit_quote_path(quote),
                class: "btn btn--light" %>
  </div>
</div>