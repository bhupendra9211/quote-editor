<main class="container mx-auto max-w-4xl p-6 bg-white rounded-lg shadow-md">
  <%= link_to sanitize("&larr; Back to quote"), quote_path(@quote), class: "text-blue-600 hover:text-blue-800 font-semibold" %>

  <div class="header mt-4 mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Edit Quote</h1>
  </div>

  <%= render "form", quote: @quote %>
</main>
